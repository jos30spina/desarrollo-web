un callbacks son funciones que se pasan como argumentos a otras funciones. se utilizan para ejecutar codigo despues de que se haya completado la tarea.

promesas una promesa es un objeto que represeta la terminacion o el fracaso eventual de una operacion asincrona.

// el promise tiene tres estados:
// 1 pending estado inicial, no cumplida o rechazada.
// 2 resolved significa que la operacion se completo.
// 3 reject: significa que la operacion fallo.
